func fibonacci(n) {
    let fibSequence = [0, 1]
    for i in range(2, n) {
        let nextValue = fibSequence[-1] + fibSequence[-2]
        call push(fibSequence, nextValue)
    }
    return fibSequence[-1]
}

print(fibonacci(10))
